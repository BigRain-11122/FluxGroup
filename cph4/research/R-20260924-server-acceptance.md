# R-20260924-server-acceptance · 服务器端本地实测独立复核记录（防线二·CPH4 Labs）

> 溯源：P-53 复核门（「本地测好=购买与部署前置门」）·复核人=实验室（独立于承建方 BigDomain）·复核=2026-09-24 晚·方法=**全新进程复跑全部 AC 套件（不采信自报）+实验室补测承建方未做的全量 census 万行级**。

## 复核结果（六面全绿）

| 套件 | 判据 | 独立复跑结果 | 关键结构判据实证 |
|---|---|---|---|
| 大厅 lobby | 13/13 | PASS·exit 0 | **`:ro` 写试拒+文件零改动**（结构性单写者实证）·census 白名单 12 键零深水泄漏·荣誉席零渲染·化身 intake evt_id 全链+闸拒·no-pong 3.0s 断开 |
| 代币账本 ledger | 11/11 | PASS·exit 0 | 复式恒等+超额触发器拒·同笔重放/同源二发拒·篡改注入对账 exit2 FAIL 正确 |
| UGC 管道 ugc | 12/12 | PASS·exit 0 | 三层筛查状态机·终稿不自动采用·拒绝仅记录零回执 |
| 支付 pay | 16/16 | PASS·exit 0 | 闸未接线拒启 E_GATE_OFFLINE（无凭证不接线的合规姿态正确）·对账面 |
| 权益月卡 member | 16 判据/25 断言 | 0 失败·exit 0 | 禁档文案拒·出生证永续·续费起点=max(now,end)·过期清扫 sweep |
| **census 万行级（实验室补测）** | p95<100ms | **PASS：10,003 行导入 0.11s·查询 p50=0.010ms/p95=0.015ms**（判据余量 ~6600×） | 真文件=BigLife census 全量·真接口=lobby store 导入/查询 |

AC-1（bootstrap bash -n）已于本批先行通过；城市数据件在位（census 样例+world-public 快照四键）。

## 诚实注记

- 100 连接×300s 全量压测未在本次复核复跑（套件 gate 未触发）——承建方自证两次在案（p95=32.3ms 与 7.4ms·均远低于 200ms 判据）；
- msgSecCheck/微信支付真实对接=按设计**拒启**（无 CEO 凭证不接线）——上线时接真件属预期态；
- 复核环境=bm-a 原生 Python 3.14（P-53 注记的最简化律路线）。

## 结论

**本地测好判据全绿——P-53 前置门通过，服务器端代码=部署级就绪**（到手 IP→bootstrap→沙箱件挂载→试运行的链路无代码阻塞）。

## 购买建议（P-53 承诺随门通过复活）

购买卡（infra-6 §1.1·**腾讯云轻量·锐驰型 2核2G·上海·459 元/年**）生效**待 CEO 下单**——一句话即启动部署链：到手 IP→`bootstrap-server.sh --baseline-only`（安全基线）→city 扩展启用→备案期 IP 内网试运行。
