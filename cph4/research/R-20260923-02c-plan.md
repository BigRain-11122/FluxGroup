# R-20260923-02c 治理层 v2 · 方案件（呈批稿）

> 溯源：CEO 令 2026-09-23 ~22:40 四步流程之步二。依据=本目录 02a（内部审计）+02b（外部对标）+02（章程·七条验收线）。**本件呈批+专家三视角评审后经 CEO 放行才落地，落地分批禁 big-bang。**

## 一、目标态：三层文本面（Thin-Context Architecture）

| 层 | 载体 | 预算 | 规则 |
|---|---|---|---|
| **L0 薄卡层**（自动注入·每会话必载） | 根/线/产品 CODELY.md=索引卡；AI.md | **≤200 行/25KB**（Claude 官方同款截断值） | 一行一条（≤200 chars）：指针+一句话；**零叙述** |
| **L1 登记处层**（机器可读·唯一事实源） | `registry/org.yaml`（实体）/`registry/orders.jsonl`（一令一行）/`registry/proposals.jsonl`（P 件）/`registry/machines.yaml`（机队） | 行级追加·滚动归档 | **事实只在此登记一次**；探针/轮次/生成器直接消费 |
| **L2 叙述层**（人读·按需取） | governance/RULES 等章程（法条保留散文）；尾部视图/周报夜报/面板=派生 | 法条内**事实表全转指针**到 L1 | 禁复述：引用一律 `O-id`/`P-id`/`org.yaml 键`，禁引原文 |

**复述禁令**（治 3-4 遍复述）：CEO 原话全文只存 orders.jsonl 一处；commit 信息=溯源载体（semantic-release 模式），台账视图行自动派生；记忆条目=一行指针制（长件转主题文件，append 工具原生支持）。

## 二、尺寸预算与体量三律（验收线 5）

- 水位三档：**警告**（夜轮点名）/**归档**（>30 天活动窗自动转 `registry/archive/`）/**阻断**（新 commit 超 L0 预算=selfaudit FAIL）。
- selfaudit.ps1 加 size-lint 段（每文件水位+超线清单）→ 周报「资源健康」姊妹节「文本健康」；夜轮近限提醒（Claude 官方近限提醒同款）。
- 首版水位（波A 数字定标）：根记忆索引卡 200 行；orders 活动窗 30 天；ledger 提案区开放件 ≤40 行；产品 CODELY 治理红线=**>25KB 视为 P1 健康问题转办**（BigMoney 10.5 万 tok 即此例）。

## 三、迁移分批案（rainbow 式·每批一 commit 独立 revert·验收线 6/7）

| 批 | 内容 | 影子读/判据 | 风险 |
|---|---|---|---|
| **批1 pilot** | orders.md→orders.jsonl：①存量 30+ 行一次性转 jsonl（id/ts/quote/落点/状态/溯源六字段）②尾部人读视图=生成器产出（保留 CEO 可读格式）③orders_hq 探针加 jsonl 消费**影子模式** | 双源对比一夜全等→切主源；判据=探针双源一致+夜轮/CityWatch 消费零红 | 最低（单文件+探针可控） |
| **批2 org.yaml** | 吸收 BRAND §8+governance §2 登记簿+architecture 线表+fleet-allocations 四处→一处登记；四地转「生成视图或 3 行指针」 | 开线演练：加一行（BigLife 案例重演）→生成器跑通三处视图 | 涉 T0 面（BRAND）——迁移本身一次显式批 |
| **批3 记忆索引卡** | 根 CODELY.md 存量 49 条→主题文件分档+索引卡 ≤200 行（按 02a §8 分组：组织/机制/元宙/待办） | 自动注入实测 <2k tok；按主题检索可用 | 存量一次性归档·夜轮前完成备份 |
| **批4 proposals.jsonl** | ledger 提案区 P 件转 jsonl+周轮/DevLoop 自领链路切读 | DevLoop 自领一致+周轮报告派生 | 与批1 同构 |
| **批5 记忆纪律+三司转办** | append 引用模式立法入 governance §5；BigMoney/BigStream/MiniGame 记忆治理=各司自领转办件（BigMoney 那件=P1） | 三司回执 | 各司自治面·集团只立法不代改 |

**批间纪律**：每批前 verify 快照、批后 24h 观察窗（探针/轮次零红）才开下一批；任何一批可独立 revert（versioning.md 追加式回滚同源）。

## 四、专家评审安排（章程 §五·含效率平衡律）

- 三视角**独立并行**（互不见稿·各出一 R-…-review-{1,2,3}.md·SLA 24h·返工 ≤2 轮超限升 CEO 裁）：
  ①**架构与扩展性**：L0/L1/L2 分层是否闭合、org.yaml schema 是否扛得住第 6-10 家子公司；
  ②**安全与故障域**：影子读断供、生成器故障、jsonl 损坏的自愈路径；密钥/敏感面不进 jsonl（安全律复核）；
  ③**三司接入成本**：批1-5 对各司轮次/探针/协议的改动清单+每司「多花/少花多少」对账。
- **CEO 终审一句话放行** → 按批执行。
- 评审底判据=七条验收线逐条打分+02a 数字前后对照承诺（冷启动 3 万→目标 **≤4k tok**；开线 6-10 文件→**1 行登记+自动 changelog**；复述面 **60-70% 削减**）。

## 五、风险清单（诚实律全列）

1. 双写期两制并存混乱→批内共存约定+影子读；2. CEO 直读 orders.md 习惯→尾部人读视图保真；3. 探针切源漏感→影子一夜全等才切；4. 生成器成为单点→生成器本身进 selfaudit 门禁；5. 记忆索引卡丢失细节→主题文件保留全文+git 史兜底；6. BigMoney 记忆治理涉产品自治→只转办不代改；7. 「立国日斜率」外推误差→周轮复测校准水位。

## 六、本件自检（体量纪律示范）

- 本方案件 92 行≤100 行；全部建议可执行（无「应考虑」）；每个数字有 02a 实测出处；迁移零 big-bang、每批可独立回滚。
